Replace pyannote diarization with faster-whisper gap analysis; add diversity pipeline, resilience upgrades, and UI fixes

- clip_engine/speaker_analysis.py: rewrite to detect speaker turns via
  faster-whisper word-level timestamps (gap >= 0.8s threshold); remove
  all pyannote / HF_TOKEN dependencies; keep identical public API
- clip_studio_app.py: update diarization button to "Detect Speakers
  (faster-whisper)", remove HF_TOKEN mention, add WAV fallback search
  under outputs/; remove hardcoded value= from cs_ widgets (value=20,
  value=60, value=45) so Streamlit reads defaults from session state
- clip_engine/clip_diversity.py: expanded diversity pipeline with
  timeline-region balancing and similarity dedup
- clip_engine/clip_pipeline.py: major refactor with gpu pipeline path
  and richer candidate scoring
- clip_engine/openai_resilience.py: rate-limit retry, token-saver mode,
  JSON fallback for GPT responses
- clip_engine/clip_analysis.py, clip_discovery.py: signal scoring
  improvements and local candidate discovery hooks
- clip_engine/export_vertical.py, ffmpeg_gpu.py: NVENC probe hardening
- clip_engine/transcription.py, transcription_utils.py: sentence-merge
  and faster-whisper model size control
- New modules: ai_profiles, clip_split_parts, effective_config,
  gpu_pipeline, local_candidate_discovery, semantic_ranking, telemetry
- launch_ai_clip_studio.ps1: venv and env-var startup improvements

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
